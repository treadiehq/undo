{
  "name": "undo-ui",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt generate && rm -rf dist && cp -R .output/public dist",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "nuxt": "^4.0.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.5.0",
    "vue": "^3.5.0"
  },
  "dependencies": {
    "@pierre/diffs": "^1.3.4"
  }
}
